{
  "name": "@minf-commerce/database",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "yarn db:generate && tsc",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^4.16.0"
  },
  "devDependencies": {
    "prisma": "^4.16.0",
    "typescript": "^5.1.3"
  }
}
